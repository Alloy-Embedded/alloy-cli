# Tasks — add-quickstart-and-cookbook

## Phase 1: Quickstart

- [ ] 1.1 `docs/QUICKSTART.md` walkthrough: install → new →
      build → flash → verify on a real Nucleo-G071RB.
- [ ] 1.2 Embed annotated SVGs from `docs/images/` at each
      step.
- [ ] 1.3 README hero badge points at QUICKSTART (above the
      fold).

## Phase 2: Progressive examples

- [ ] 2.1 `docs/EXAMPLES/01-blinky/`: GPIO + Board::init.
- [ ] 2.2 `docs/EXAMPLES/02-uart-echo/`: typed UART add.
- [ ] 2.3 `docs/EXAMPLES/03-spi-flash/`: SPI driver against
      `at25` synthetic part.
- [ ] 2.4 `docs/EXAMPLES/04-dma-double-buffer/`: UART RX +
      `suggest_dma_pair` auto-allocation.
- [ ] 2.5 Each example has a `README.md`, a complete
      `alloy.toml`, and the generated `peripherals.cpp`.

## Phase 3: Error cookbook

- [ ] 3.1 `docs/ERROR_COOKBOOK.md` — one section per
      `AlloyCliError.error_type`.
- [ ] 3.2 Each section: trigger / message / fix / related MCP
      tool.  Cross-linked from CLI/TUI messages.
- [ ] 3.3 CI job validates that every `error_type` declared in
      `core.errors` has a matching anchor in the cookbook.

## Phase 4: Cheatsheet + scaffold helper

- [ ] 4.1 `docs/CHEATSHEET.md` generated by `scripts/
      generate_cheatsheet.py` walking the Click command tree.
- [ ] 4.2 CI runs the script in `--check` mode and fails when
      the file would change.
- [ ] 4.3 `alloy new --from-example <name>` reads the
      example's `alloy.toml` and scaffolds a working project
      under the user's chosen path.

## Phase 5: Tests

- [ ] 5.1 Markdown lint (`pymarkdown` or `markdownlint-cli`)
      runs against `docs/**/*.md` in CI.
- [ ] 5.2 Each example's `alloy.toml` parses through
      `core.project.parse` without diagnostics.
- [ ] 5.3 `alloy new --from-example 01-blinky` test produces
      a project tree whose `alloy.toml` matches the example's.

## Phase 6: Spec + final checks

- [ ] 6.1 Spec deltas in `specs/developer-experience/spec.md`
      (new capability).
- [ ] 6.2 `openspec validate add-quickstart-and-cookbook
      --strict` passes.
