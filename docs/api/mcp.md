# `alloy_cli.mcp`

The MCP transport surface — `ToolRegistry` + every typed `Tool`
exposed to LLM agents (Claude / opencode / Cursor / Continue).
For the LLM-facing system prompt + canonical workflows head to
[AI integration](../AI_INTEGRATION.md).

::: alloy_cli.mcp
    options:
      show_root_heading: false
      show_root_toc_entry: false
      members_order: source
      heading_level: 2
